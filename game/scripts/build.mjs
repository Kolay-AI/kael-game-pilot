import{cp,mkdir,rm}from'node:fs/promises';import{fileURLToPath}from'node:url';import path from'node:path';const root=path.resolve(path.dirname(fileURLToPath(import.meta.url)),'..'),out=path.join(root,'dist');await rm(out,{recursive:true,force:true});await mkdir(out);for(const f of['index.html','style.css','game.mjs','rules.mjs','gameplay.mjs','level-data.mjs','art.mjs'])await cp(path.join(root,f),path.join(out,f));await mkdir(path.join(out,'sprites'),{recursive:true});for(const f of['kael-idle.png','kael-walk.png','kael-melee.png','kael-takeoff.png','kael-jumpUp.png','kael-doubleJump.png','kael-fall.png','kael-land.png'])await cp(path.join(root,'sprites',f),path.join(out,'sprites',f));console.log('Build: game/dist');
